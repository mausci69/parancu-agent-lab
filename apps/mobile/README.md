# EcoSearch Mobile (Expo)

## 1) Install prerequisites
- Node.js 18+ (`node -v` to confirm)
- Android Studio or Xcode if you want a device emulator
- (Optional) Expo CLI: `npm i -g expo`

## 2) Install project dependencies
```bash
cd apps/mobile
npm install
