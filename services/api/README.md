# services/api

EcoSearch FastAPI service (backend API).
This folder is created as part of the monorepo skeleton.

> Next steps (in separate commits):
> - Add Python .gitignore
> - Create minimal FastAPI app (api/main.py) with /health and /version
> - Add requirements.txt (pinned later) and run instructions

